# Django Settings
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database
DB_NAME=product_service_db
DB_USER=postgres
DB_HOST=db
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8002

# Email (Console for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@ecommerce.com

# JWT Settings (MUST match User Service for cross-service auth)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# Product Service Specific
PRODUCT_IMAGE_MAX_SIZE_MB=5
PRODUCT_IMAGE_ALLOWED_EXTENSIONS=jpg,jpeg,png,webp
